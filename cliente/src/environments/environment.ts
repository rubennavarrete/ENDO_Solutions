// Servidor local
const baseUrl = '';
export const environment = {
  production: false,
  baseUrl: baseUrl,
  url: 'http://localhost:4400/' + baseUrl,
  urlLogOut: 'http://localhost:4400/' + baseUrl,
  urlApi: 'http://localhost:4020',
};
